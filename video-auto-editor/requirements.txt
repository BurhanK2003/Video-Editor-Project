moviepy==1.0.3
pillow==11.3.0
numpy==2.4.3
pydub==0.25.1
imageio-ffmpeg==0.6.0
faster-whisper==1.1.1
librosa==0.11.0
scenedetect==0.6.7
opencv-python==4.11.0.86
faiss-cpu==1.13.2

# Optional for async online fetch (Pexels/Pixabay). If install fails on Windows,
# pipeline falls back to local-only matching for that run.
# aiohttp==3.11.13

# --- Optional: AI upgrades (install for best quality) ---

# CLIP visual B-roll matching — matches actual video frames to scene descriptions.
# pip install transformers torch
# transformers
# torch

# Sentence-transformer text matching (fallback when CLIP is unavailable).
# pip install sentence-transformers
# sentence-transformers

# Optional YouTube Creative Commons stock fetch provider.
# pip install yt-dlp
# yt-dlp

# Faster Whisper transcription (better accuracy & speed than base whisper).
# pip install faster-whisper
# faster-whisper

# --- Optional: Free TTS for script-to-video ---

# Kokoro ONNX — best local TTS, no internet required.
# pip install kokoro-onnx soundfile
# kokoro-onnx
# soundfile

# Edge TTS — Microsoft neural voices, free, internet required, no API key.
# pip install edge-tts
# edge-tts

# pyttsx3 — fully offline fallback, lower quality.
# pip install pyttsx3
# pyttsx3
