# Caption Fonts

Place open-source font files in this folder for the caption engine:

- `Montserrat-Bold.ttf`
- `Anton-Regular.ttf`

The renderer tries these bundled fonts first and falls back to system fonts if missing.
